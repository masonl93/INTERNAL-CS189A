$('#msgList').prepend($("<%= j render partial: 'ajaxchat', :locals => {:image => @chats.user.image, :name => @chats.user.name, :created => @chats.created_at.strftime('%-d %B %Y, %H:%M:%S'),:body => @chats.body }%>").hide().fadeIn(1000));
$('#chatBody').val("");